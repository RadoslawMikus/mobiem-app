export class Format {
  constructor(
    public title: string,
    public description: string,
    public url: string
  ) {
    this.title = title;
    this.description = description;
    this.url = url;
  }
}
